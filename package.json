{
  "name": "flowbond-docs",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy:test": "pnpm build && wrangler pages deploy dist --project-name=flowbond-docs --branch=test",
    "deploy:prod": "pnpm build && wrangler pages deploy dist --project-name=flowbond-docs --branch=main"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.2",
    "@astrojs/starlight": "^0.41.7",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "astro": "^7.2.2",
    "mermaid": "^11.16.1",
    "sharp": "^0.35.3"
  }
}
